# srsRAN_4G
## for testing
```
mkdir build && cd build
cmake ../
make -j18
```


```./lib/examples/cell_search -b 1 -a type=x300,time_source=gpsdo -s 94 -e 104
./lib/examples/pdsch_ue -f 2120e6 -d -E 100
mkdir cache && mkdir cache/band_1 && mkdir cache/band_1/cell_420
cp ../output/* cache/band_1/cell_420/
./lib/test/common/gen_sample --type sib1_tac -c 420
./lib/test/common/gen_sample --type sib2_acbarring -c 420
./lib/test/common/gen_sample --type paging_sysinfmod -c 420
./lib/test/common/gen_sample --type paging_imsi -c 420 -m 460017837217696

```
### X310
```
Paing IMSI
sudo ./lib/examples/lte_sync_loop_tx \
                                    -f 2120000000 \
                                    -d UHD \
                                    -a "type=x300" \
                                    -g 30 \
                                    -t 30 \
                                    -m paging_imsi \
                                    -u 100 \
                                    -Q \
                                    -B rf
Paging SIB2
sudo ./lib/examples/lte_sync_loop_tx \
                                    -f 2120000000 \
                                    -d UHD \
                                    -a "type=x300" \
                                    -g 30 \
                                    -t 30 \
                                    -m paging_sib2 \
                                    -u 100 \
                                    -Q \
                                    -B rf
Paging SIB1 (commit paging sib1 seg tx)
sudo ./lib/examples/lte_sync_loop_tx \
                                    -f 2120000000 \
                                    -d UHD \
                                    -a "type=x300" \
                                    -g 30 \
                                    -t 30 \
                                    -m paging_sib1 \
                                    -u 100 \
                                    -Q \
                                  -B rf
```
### B210
需要功率放大器
```
sudo ./lib/examples/lte_sync_loop_tx \
                                    -f 2120000000 \
                                    -d UHD \
                                    -a "type=b200,master_clock_rate=30.72e6,smaple_rate=30.72e6" \
                                    -g 90 \
                                    -t 90 \
                                    -m paging_imsi \
                                    -u 100 \
                                  -Q
```  
## for LTESniffer
```
cd LTESniffer
mkdir build && cd build
cmake .. -DENABLE_APPS=ON
make -j18
```
